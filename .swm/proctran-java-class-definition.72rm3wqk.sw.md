---
title: PROCTRAN Java Class Definition
---
# Introduction

This document will walk you through the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="14:4:4" line-data="public class PROCTRAN {">`PROCTRAN`</SwmToken> Java class definition.

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="14:4:4" line-data="public class PROCTRAN {">`PROCTRAN`</SwmToken> class is designed to handle COBOL data structures in Java. It uses the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="15:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken> to manage COBOL data types and their offsets within a byte buffer. The class provides methods to get and set various fields within the COBOL structure.

We will cover:

1. Initialization of the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="15:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken>
2. Handling of specific COBOL fields
3. Methods for accessing and modifying data

# Initialization of the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="15:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="12">

---

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="15:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken> is initialized to handle COBOL data types. This factory is used throughout the class to define and manipulate COBOL fields.

```java
// Generated by IBM Record Generator for Java V3.0.2 Build 20221206-1121 on: Tue Feb 14 07:30:53 CST 2023

public class PROCTRAN {
	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();
	static { factory.setStringTrimDefault(false); } 

	/** <pre>
	 01 COBOL-LANGUAGE-STRUCTURE. </pre> */
	public static final int COBOL_LANGUAGE_STRUCTURE_LEN = 99; 
```

---

</SwmSnippet>

# Handling of specific COBOL fields

The class defines various COBOL fields using the factory. These fields are mapped to specific offsets and lengths within the byte buffer.

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="34">

---

For example, the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="36:9:9" line-data="	protected static final StringField PROC_TRAN_EYE_CATCHER = factory.getStringField(4);">`PROC_TRAN_EYE_CATCHER`</SwmToken> field is defined as a 4-character string field.

```java
	/** <pre>
	        05 PROC-TRAN-EYE-CATCHER        PIC X(4). </pre> */
	protected static final StringField PROC_TRAN_EYE_CATCHER = factory.getStringField(4);
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="50">

---

Similarly, the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="52:9:9" line-data="	protected static final StringField PROC_TRAN_LOGICAL_DELETE_FLAG = factory.getStringField(1);">`PROC_TRAN_LOGICAL_DELETE_FLAG`</SwmToken> is defined as a 1-character string field.

```java
	/** <pre>
	           07 PROC-TRAN-LOGICAL-DELETE-FLAG PIC X. </pre> */
	protected static final StringField PROC_TRAN_LOGICAL_DELETE_FLAG = factory.getStringField(1);
```

---

</SwmSnippet>

# Methods for accessing and modifying data

The class provides methods to get and set the values of the COBOL fields. These methods interact with the byte buffer to read and write data.

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="458">

---

For instance, the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="458:5:5" line-data="	public String getProcTranEyeCatcher() {">`getProcTranEyeCatcher`</SwmToken> method retrieves the value of the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="459:3:3" line-data="		return PROC_TRAN_EYE_CATCHER.getString(byteBuffer);">`PROC_TRAN_EYE_CATCHER`</SwmToken> field from the byte buffer.

```java
	public String getProcTranEyeCatcher() {
		return PROC_TRAN_EYE_CATCHER.getString(byteBuffer);
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="462">

---

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="462:5:5" line-data="	public void setProcTranEyeCatcher(String procTranEyeCatcher) {">`setProcTranEyeCatcher`</SwmToken> method sets the value of the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="463:1:1" line-data="		PROC_TRAN_EYE_CATCHER.putString(procTranEyeCatcher, byteBuffer);">`PROC_TRAN_EYE_CATCHER`</SwmToken> field in the byte buffer.

```java
	public void setProcTranEyeCatcher(String procTranEyeCatcher) {
		PROC_TRAN_EYE_CATCHER.putString(procTranEyeCatcher, byteBuffer);
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="486">

---

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="486:5:5" line-data="	public int getProcTranSortCode() {">`getProcTranSortCode`</SwmToken> method retrieves the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="488:5:5" line-data="			procTranSortCode = PROC_TRAN_SORT_CODE.getInt(byteBuffer);">`PROC_TRAN_SORT_CODE`</SwmToken> field value, caching it for performance.

```java
	public int getProcTranSortCode() {
		if (!procTranSortCodeIsSet) {
			procTranSortCode = PROC_TRAN_SORT_CODE.getInt(byteBuffer);
			procTranSortCodeIsSet = true;
		}
		return procTranSortCode;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="494">

---

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="494:5:5" line-data="	public void setProcTranSortCode(int procTranSortCode) {">`setProcTranSortCode`</SwmToken> method sets the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="495:8:8" line-data="		if (procTranSortCodeIsSet &amp;&amp; PROC_TRAN_SORT_CODE.equals(this.procTranSortCode, procTranSortCode)) {">`PROC_TRAN_SORT_CODE`</SwmToken> field value and updates the cache.

```java
	public void setProcTranSortCode(int procTranSortCode) {
		if (procTranSortCodeIsSet && PROC_TRAN_SORT_CODE.equals(this.procTranSortCode, procTranSortCode)) {
			return;
		}
		PROC_TRAN_SORT_CODE.putInt(procTranSortCode, byteBuffer);
		this.procTranSortCode = procTranSortCode;
		procTranSortCodeIsSet = true;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="1020">

---

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="1020:5:5" line-data="	public BigDecimal getProcTranAmount() {">`getProcTranAmount`</SwmToken> method retrieves the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="1022:5:5" line-data="			procTranAmount = PROC_TRAN_AMOUNT.getBigDecimal(byteBuffer);">`PROC_TRAN_AMOUNT`</SwmToken> field value as a <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="1020:3:3" line-data="	public BigDecimal getProcTranAmount() {">`BigDecimal`</SwmToken>.

```java
	public BigDecimal getProcTranAmount() {
		if (procTranAmount == null) {
			procTranAmount = PROC_TRAN_AMOUNT.getBigDecimal(byteBuffer);
		}
		return procTranAmount;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" line="1027">

---

The <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="1027:5:5" line-data="	public void setProcTranAmount(BigDecimal procTranAmount) {">`setProcTranAmount`</SwmToken> method sets the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/PROCTRAN.java" pos="1028:4:4" line-data="		if (PROC_TRAN_AMOUNT.equals(this.procTranAmount, procTranAmount)) {">`PROC_TRAN_AMOUNT`</SwmToken> field value.

```java
	public void setProcTranAmount(BigDecimal procTranAmount) {
		if (PROC_TRAN_AMOUNT.equals(this.procTranAmount, procTranAmount)) {
			return;
		}
		PROC_TRAN_AMOUNT.putBigDecimal(procTranAmount, byteBuffer);
		this.procTranAmount = procTranAmount;
	}

}
```

---

</SwmSnippet>

These methods ensure that the data within the byte buffer is accurately represented and manipulated according to the COBOL structure.

&nbsp;

*This is an auto-generated document by Swimm 🌊 and has not yet been verified by a human*

<SwmMeta version="3.0.0" repo-id="Z2l0aHViJTNBJTNBY2ljcy1iYW5raW5nLXNhbXBsZS1hcHBsaWNhdGlvbi1jYnNhLUlCTS1EZW1vJTNBJTNBU3dpbW0tRGVtbw==" repo-name="cics-banking-sample-application-cbsa-IBM-Demo"><sup>Powered by [Swimm](/)</sup></SwmMeta>

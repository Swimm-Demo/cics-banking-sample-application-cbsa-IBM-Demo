---
title: CRECUST Class Definition
---
# Introduction

This document will walk you through the CRECUST class definition in the <SwmPath>[src/…/datainterfaces/CRECUST.java](src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java)</SwmPath> file.

The CRECUST class is designed to handle customer data in a COBOL-based system. It uses the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="14:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken> to manage various fields and their offsets within a byte buffer.

We will cover:

1. Initialization of the class and its fields.
2. Handling of specific fields and their getters and setters.
3. Management of the byte buffer.

# Class initialization

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="11">

---

The CRECUST class is initialized with a <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="14:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken> to handle COBOL data types. This factory is configured to not trim strings by default.

```java
// Generated by IBM Record Generator for Java V3.0.2 Build 20221206-1121 on: Tue Feb 14 06:51:17 CST 2023

public class CRECUST {
	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();
	static { factory.setStringTrimDefault(false); } 

	/** <pre>
	 LINKAGE SECTION. </pre> */
	public static final int DFHCOMMAREA_LEN = 261; 
```

---

</SwmSnippet>

# Field definitions

The class defines several fields using the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="14:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken>. These fields correspond to COBOL data types and are used to manage customer data.

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="21">

---

For example, the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="31:9:9" line-data="	protected static final StringField COMM_EYECATCHER = factory.getStringField(4);">`COMM_EYECATCHER`</SwmToken> field is defined as a string of length 4:

```java
	/** <pre>
	 01 DFHCOMMAREA.
	     COPY CRECUST.
    *
    *
    *    Copyright IBM Corp. 2023
    *
    *
    *
	    03 COMM-EYECATCHER                 PIC X(4). </pre> */
	protected static final StringField COMM_EYECATCHER = factory.getStringField(4);
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="39">

---

Another example is the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="41:9:9" line-data="	protected static final ExternalDecimalAsIntField COMM_SORTCODE = factory.getExternalDecimalAsIntField(6, false, false, false, false);">`COMM_SORTCODE`</SwmToken> field, which is defined as an external decimal integer of length 6:

```java
	/** <pre>
	    03 COMM-EYECATCHER                 PIC X(4). </pre> */
	protected static final ExternalDecimalAsIntField COMM_SORTCODE = factory.getExternalDecimalAsIntField(6, false, false, false, false);
```

---

</SwmSnippet>

# Byte buffer management

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="130">

---

The class uses a byte buffer to store the raw data. This buffer is either provided during instantiation or created with a default length.

```java
	public CRECUST (byte[] buffer) {
		this.byteBuffer = buffer;
	}
```

---

</SwmSnippet>

# Getters and setters

The class provides getters and setters for each field. These methods handle the conversion between the byte buffer and the field values.

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="143">

---

For example, the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="143:5:5" line-data="	public String getCommEyecatcher() {">`getCommEyecatcher`</SwmToken> method retrieves the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="145:5:5" line-data="			commEyecatcher = COMM_EYECATCHER.getString(byteBuffer);">`COMM_EYECATCHER`</SwmToken> field from the byte buffer:

```java
	public String getCommEyecatcher() {
		if (commEyecatcher == null) {
			commEyecatcher = COMM_EYECATCHER.getString(byteBuffer);
		}
		return commEyecatcher;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="150">

---

The corresponding setter method, <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="150:5:5" line-data="	public void setCommEyecatcher(String commEyecatcher) {">`setCommEyecatcher`</SwmToken>, updates the byte buffer with the new value:

```java
	public void setCommEyecatcher(String commEyecatcher) {
		if (COMM_EYECATCHER.equals(this.commEyecatcher, commEyecatcher)) {
			return;
		}
		COMM_EYECATCHER.putString(commEyecatcher, byteBuffer);
		this.commEyecatcher = commEyecatcher;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="158">

---

Similarly, the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="158:5:5" line-data="	public int getCommSortcode() {">`getCommSortcode`</SwmToken> method retrieves the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="160:5:5" line-data="			commSortcode = COMM_SORTCODE.getInt(byteBuffer);">`COMM_SORTCODE`</SwmToken> field:

```java
	public int getCommSortcode() {
		if (!commSortcodeIsSet) {
			commSortcode = COMM_SORTCODE.getInt(byteBuffer);
			commSortcodeIsSet = true;
		}
		return commSortcode;
	}
```

---

</SwmSnippet>

<SwmSnippet path="/src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" line="166">

---

And the <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="166:5:5" line-data="	public void setCommSortcode(int commSortcode) {">`setCommSortcode`</SwmToken> method updates the byte buffer with the new sort code:

```java
	public void setCommSortcode(int commSortcode) {
		if (commSortcodeIsSet && COMM_SORTCODE.equals(this.commSortcode, commSortcode)) {
			return;
		}
		COMM_SORTCODE.putInt(commSortcode, byteBuffer);
		this.commSortcode = commSortcode;
		commSortcodeIsSet = true;
	}
```

---

</SwmSnippet>

# Conclusion

This document covered the main aspects of the CRECUST class, including its initialization, field definitions, byte buffer management, and getters and setters. The class is designed to handle customer data in a COBOL-based system, using a <SwmToken path="src/webui/src/main/java/com/ibm/cics/cip/bankliberty/datainterfaces/CRECUST.java" pos="14:5:5" line-data="	protected static CobolDatatypeFactory factory = new CobolDatatypeFactory();">`CobolDatatypeFactory`</SwmToken> to manage various fields and their offsets within a byte buffer.

&nbsp;

*This is an auto-generated document by Swimm 🌊 and has not yet been verified by a human*

<SwmMeta version="3.0.0" repo-id="Z2l0aHViJTNBJTNBY2ljcy1iYW5raW5nLXNhbXBsZS1hcHBsaWNhdGlvbi1jYnNhLUlCTS1EZW1vJTNBJTNBU3dpbW0tRGVtbw==" repo-name="cics-banking-sample-application-cbsa-IBM-Demo"><sup>Powered by [Swimm](/)</sup></SwmMeta>
